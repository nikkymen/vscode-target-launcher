{
	"name": "vscode-cmake-launcher",
	"build": {
		"dockerfile": "Dockerfile",
		"context": "."
	},
	"features": {
		// (optional) if you want Yarn instead of npm, or Git LFS, etc.
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"dbaeumer.vscode-eslint",
				"ms-vscode.extension-test-runner",
				"ms-vscode.cmake-tools"
			]
		}
	},
	"postCreateCommand": "npm install"
}